\usepackage{tikz}
\usepackage{pgfplots}
\pgfplotsset{compat=newest}
\usepackage{pgfmath}
\usepackage{tikz-cd}
\usepackage{pgffor}
\usepackage{tkz-euclide}
\usepgfplotslibrary{fillbetween}
\usetikzlibrary{
    calc,
    angles,
    quotes,
    arrows.meta,
    math,
    backgrounds,
    pgfplots.statistics,
    matrix,
    patterns,
    shapes.geometric,
    spy,
    intersections,
    decorations.markings,
    decorations.pathmorphing,
    decorations.pathreplacing,
    decorations.shapes
}
\pgfdeclarelayer{ft}
\pgfdeclarelayer{bg}
\pgfsetlayers{bg,main,ft}
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

%%%%%%%%%%%%%%%%% Setup the Coordinate System %%%%%%%%%%%%%%%%%%%%%%
\tikzset{smooth}
\pgfplotsset{
    width=0.5\textwidth,
    % smooth,
    samples=100,
    every axis/.style={
        %		 axis equal image,
        title style={at={(0.5,-0.05)},anchor=north},
        axis lines=middle,
        unit vector ratio=1 1,
        ymajorgrids=true,
        xmajorgrids=true,
        axis line style={-latex,very thick}, % arrows on the axis
        xlabel={$x$},          % default put x on x-axis
        ylabel={$y$},          % default put y on y-axis
        xlabel style = {font=\small, at={(xticklabel* cs:1)}, anchor=south east},
        ylabel style = {font=\small, at={(yticklabel* cs:1)}, anchor=north west},
        scaled ticks=true,
        x tick label style={font=\small, inner xsep=0pt},
        y tick label style={font=\small, inner ysep=0pt},
        grid style={black},
        % set layers=standard,
    }
}
